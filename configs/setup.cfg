[flake8]
# Enforce a maximum line length of 100 characters
max-line-length = 100

# Ignore rules that frequently conflict with automated formatting
ignore = E203, W503

# Exclude operational and auto-generated directories
exclude = 
    .git,
    __pycache__,
    build,
    install,
    log